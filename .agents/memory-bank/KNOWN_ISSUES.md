# KNOWN ISSUES

1. **Legacy Navigation Mapping**: Menu header trong `PublicLayout.jsx` có link nhãn "Tín chỉ Xanh" đang trỏ tới `/citizen/track` thay vì `/community/impact`. Sẽ được sửa khi chuyển sang Community Navigation.
2. **Category Extensibility**: Cần đảm bảo UI form cho phép chọn các danh mục môi trường mở rộng ngoài bụi công trình (rác thải, đốt rơm rạ, nước thải, v.v.).
